# Database

No database is required for this project.

The System Design specifies that data is managed in-memory, via localStorage, or through file-based storage. No SQL schema or migrations are needed.
